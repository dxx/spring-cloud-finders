pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://maven.aliyun.com/repository/gradle-plugin' }
    }
}

rootProject.name = 'spring-cloud-finders'

include 'bom'
include 'spring-cloud-finders-core'
include 'spring-cloud-starter-finders'
